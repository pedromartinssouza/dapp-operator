apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dapp-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dapp-operator.labels" . | nindent 4 }}
